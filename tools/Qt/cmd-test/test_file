# a comment

TARGET a
  TARGET a
x TARGET a
COMMAND a
  COMMAND  a
x COMMAND a
EXPECT a
  EXPECT  a
x EXPECT a

a line

TARGET ls
  COMMAND ab
  
END TARGET

END TARGET a

SWITCH TO a

EXPECTE abc\sabc.*
EXPECTO abc\sabc

EXIT CODE 0
EXIT CODE NOT 0
